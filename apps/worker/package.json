{
  "name": "worker",
  "version": "0.8.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "vite dev",
    "prebuild": "pnpm --filter @line-crm/shared --filter @line-crm/line-sdk build",
    "build": "vite build",
    "preview": "vite preview",
    "predeploy": "pnpm --filter @line-crm/shared --filter @line-crm/line-sdk build",
    "deploy": "vite build && wrangler deploy",
    "typecheck": "tsc --noEmit",
    "test": "vitest run",
    "test:watch": "vitest"
  },
  "dependencies": {
    "@anthropic-ai/sdk": "^0.92.0",
    "@line-crm/db": "workspace:*",
    "@line-crm/email-sdk": "workspace:*",
    "@line-crm/line-sdk": "workspace:*",
    "@line-crm/shared": "workspace:*",
    "hono": "^4.12.16",
    "zod": "^3.24.4"
  },
  "devDependencies": {
    "@cloudflare/vite-plugin": "^1.35.0",
    "@cloudflare/workers-types": "^4.20260503.1",
    "typescript": "^5.7.0",
    "vite": "^6.4.2",
    "vitest": "^2.0.0",
    "wrangler": "^4.87.0"
  }
}
