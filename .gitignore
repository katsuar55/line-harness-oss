node_modules/
dist/
.next/
.wrangler/
.vercel/
.env
.env.local
*.log
.DS_Store
.mcp.json
*.toml.bak
.env.production
.env.staging
# カットオーバー時の本番 secret 投入用 (wrangler secret bulk)。 投入後は即削除すること。
secrets.json
secrets-*.json
*secrets*.json
# カットオーバー B-3/B-4 用の worker API_KEY (一時)。 使用後は即削除すること。
cutover-apikey.txt
*apikey*.txt
apps/web/out/
apps/worker/dist/
SESSION_HANDOFF.md
apps/worker/AI_SYSTEM_PROMPT.txt

# Auto-generated / local-only
.claude/
backups/
*.tsbuildinfo
apps/web/tsconfig.tsbuildinfo

# Playwright
apps/web/playwright-report/
apps/web/test-results/
apps/web/.playwright/

# Local-only scratch (mockups / screenshots / one-off scripts)
tmp/

# Local handoff files (not for git)
NEXT_SESSION_PROMPT.md
STAFF_GUIDE_naturism_LINE.html
# 管理システムの手引き。 本 repo は PUBLIC なので、 管理画面 URL・運用手順・権限の内訳を含む
# 社内向け資料はコミットしない (メール/LINE 添付で個別に配る)。
# スタッフ手引き (naturism 固有・連絡先等の PII を含む)。
# 記入済み版など派生ファイルも必ず除外する — 本リポジトリは public。
ADMIN_GUIDE_naturism*.html
# Shopify Flow 設定手引き (naturism の store handle・管理画面直リンクを含む社内向け資料)。
# 技術仕様は docs/TEIKI_FLOW_SETUP.md 側に置き、こちらはコミットしない。
TEIKI_FLOW_SETUP_GUIDE*.html
# 顧客向けメール文面のコピペ用資料 (naturism 固有の文面・管理画面手順を含む社内向け)。
K2_*.html
